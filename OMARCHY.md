# Earth on Omarchy

Self-contained Vite + Three.js app. No dependencies outside this folder (only npm packages via `npm install`).

```bash
cd ~/Projects/Earth
npm install
npm run dev
```

Network: internet for CelesTrak TLEs. Dev server proxies `/api/celestrak`.

Private GitHub: `Guts444/Earth` (if push succeeded).
